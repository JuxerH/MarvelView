<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.finalwork.marvelview.model.viewobject.SectionVO"/>
        <import type="com.finalwork.marvelview.adapter.ImageLoadingListener"/>
        
        <variable
            name="section"
            type="com.finalwork.marvelview.model.viewobject.SectionVO" />

        <variable
            name="imageTransition"
            type="java.lang.String" />

        <variable
            name="imageListener"
            type="ImageLoadingListener" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

        <ImageView
            android:id="@+id/image"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:transitionName="@{imageTransition}"
            sectionImageUrl="@{section.image}"
            listener="@{imageListener}"
            tools:src="@mipmap/ic_launcher" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center"
            android:lines="3"
            android:text="@{section.title}"
            android:textAlignment="center"
            android:textAppearance="@style/TextAppearance.AppCompat.Title"
            tools:text="@string/app_name" />

    </LinearLayout>

</layout>